# EIMS Setup 
1. Download the project Zip folder.
2. Extract the Zip.
3. Open the extracted project folder in VS Code.
4. Install/download the dependencies from `pom.xml` (Maven dependencies).
5. Open Terminal in VS Code and run:
.\compile.ps1
.\run.ps1

That is all.
